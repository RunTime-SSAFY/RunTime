server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: C:\msaexample\git-local-repo # TODO ? ???? yml ??? ??? git local repo? ??. ?? ???? ??