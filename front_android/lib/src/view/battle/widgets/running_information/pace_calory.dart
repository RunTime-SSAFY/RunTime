import 'package:flutter/material.dart';

class PaceCalory extends StatelessWidget {
  const PaceCalory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
