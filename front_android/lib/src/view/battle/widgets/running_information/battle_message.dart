import 'package:flutter/material.dart';

class BattleMessage extends StatelessWidget {
  const BattleMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
