import 'package:flutter/material.dart';

class RunningUnity extends StatelessWidget {
  const RunningUnity({super.key});

  // 상대가 달린 거리와 내가 달린 거리 받기

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
